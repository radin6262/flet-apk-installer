name: Build Flet APK Installer Extension Template

on:
  workflow_dispatch:

jobs:
  build-extension:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4


      # Java required by Android Gradle
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17


      # Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3


      # Install Gradle
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.10


      # Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"


      # Flutter
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Verify tools
        run: |
          java -version
          gradle --version
          flutter --version
          python --version


      # Install Flet
      - name: Install Flet
        run: |
          python -m pip install --upgrade pip
          pip install flet

      - name: Create extension directory
        run: |
          mkdir flet-apk-installer
          cd flet-apk-installer
          flet create --template extension --project-name flet-apk-installer

      - name: Install Flutter dependency
        working-directory: src/flutter/flet_apk_installer
        run: |
          flutter pub add android_package_installer


      # Build/check extension Flutter side
      - name: Build Flutter extension
        working-directory: src/flutter/flet_apk_installer
        run: |
          flutter pub get


      - name: Package extension
        run: |
          pwd
          ls -la

          zip -r flet-apk-installer.zip flet-apk-installer

      - name: Upload extension
        uses: actions/upload-artifact@v4
        with:
          name: flet-apk-installer
          path: flet-apk-installer.zip